# allow access to /media, must use with whitelist to ensure security
ignore disable-mnt

include whitelist-common.local
include mpv.local

# allow access to ranger's cache
whitelist ${HOME}/.cache/ranger

whitelist ${HOME}/mus
whitelist ${HOME}/vid
whitelist ${HOME}/pic

net none
