# vim:ft=sshconfig
# .ssh/config
# @author nate zhou
# @since 2023,2024,2025

# 1. to add keys to agent, run:
#   `systemctl enable --now --user ssh-agent.service`
# 2. in `.bash_profile` include below:
#   `export SSH_AUTH_SOCK=$XDG_RUNTIME_DIR/ssh-agent.socket`

# If multiple identify doesn't work, manually run
# `ssh-add -D`                      # remove cached keys
# `ssh-add /path/to/private/key`    # add keys
# `ssh-add -l`                      # list keys

Include unixchad.conf
Include private.conf
Include servers.conf
Include proxy.conf

Host github.com
    HostName ssh.github.com
    Port 443
    User git
    AddKeysToAgent 2419200
    IdentityFile ~/.ssh/git

Host codeberg.org
    HostName codeberg.org
    User git
    AddKeysToAgent 2419200
    IdentityFile ~/.ssh/git
