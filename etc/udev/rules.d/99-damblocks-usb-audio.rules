# udev rules to refresh damblocks audio modules with usb sound device events
# @author nate zhou
# @since 2025
# TODO: 1. action when a device is added (not working on my system)
#       2. reload sink as well (script need update)

ACTION=="remove", SUBSYSTEMS=="usb", SUBSYSTEM=="sound", RUN+="/usr/bin/sudo -u nate /bin/sh -c 'XDG_RUNTIME_DIR=/run/user/1000 XDG_CACHE_HOME=/home/nate/.cache /home/nate/.local/bin/audio source --reload'"
